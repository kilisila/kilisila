# CLAUDE.md — 引き継ぎメモ

このファイルは、チャット上での設計フェーズから Claude Code へ作業を引き継ぐための申し送りです。コードを読むだけでは分からない「なぜこうなっているか」と「どこが未検証か」を記載します。

---

## 1. このアプリの目的

テレビで YouTube を視聴しながら、手元の Android 端末にコメント欄だけを大きく表示する。ライブ配信とアーカイブの両方に対応する。**利用者本人の個人利用が前提**であり、ストア公開は想定しない（理由は第 6 節）。

対象端末は Xiaomi 13T Pro（HyperOS）。開発機は Windows PC または MacBook。

---

## 2. 採用した設計と、その理由

### 方式：ポップアウトチャットの WebView 表示

`https://www.youtube.com/live_chat?is_popout=1&v=<動画ID>` を WebView でトップレベル読み込みする。

**この方式を選んだのは、他の案を検討して却下した結果です。安易に作り直さないでください。**

- **YouTube Data API 方式を却下した理由**：`liveChatMessages.list` は 1 回 5 ユニット、既定の割り当ては 1 日 10,000 ユニット。5 秒間隔のポーリングで 1 時間あたり 3,600 ユニット消費するため、3 時間弱で枯れる。長時間配信の視聴という用途に耐えない。
- **innertube（非公式エンドポイント）方式を却下した理由**：割り当ての問題は解消するが、継続トークンの取得が壊れやすく、規約上の位置づけも悪い。
- **ポップアウト方式の利点**：同期処理を自前で持たなくてよい。スーパーチャット、メンバーシップ、カスタム絵文字、モデレーター表示がすべて正しく描画される。YouTube 側の仕様変更に強い。

### デスクトップの利用者エージェント文字列を名乗っている

`ChatActivity.DESKTOP_UA` は意図的な設定です。携帯端末として振る舞うと簡易版へ誘導され、ポップアウトチャットが正しく表示されない懸念があるため。**動作確認なしに削除しないでください。**

### アーカイブ同期の仕組み

チャットのリプレイは、動画プレイヤーから送られる再生位置の通知を見て進む。通知の形式は以下。

```json
{"event":"infoDelivery","info":{"currentTime":754.0,"playerState":1},"id":1}
```

`ReplaySync` が仮想の再生時計を持ち、`ChatActivity.pushPlayerTime()` が同形式の通知を 0.5 秒ごとにページへ送り込む。利用者はテレビに表示されている再生時間を入力するだけでよい。

テレビ側の再生位置は取得できないため、自動連動はしない。手動合わせ＋`±10` 秒の微調整という仕様は、技術的制約を受け入れた上での意図的な設計です。

---

## 3. 検証済みの範囲と、未検証の範囲

### 検証済み

- `TimeText`、`VideoId`、`ReplaySync` は Kotlin コンパイラ 1.9.24 で実際にコンパイルし、単体テストが全件通過。テストは `app/src/test/` に同梱
- `MainActivity`、`ChatActivity`、`Prefs` はフレームワークのスタブに対して型検査済み（エラーなし）
- 注入する JavaScript は疑似 DOM 上で実行し、構文・送信内容・スタイル要素の非重複を確認
- レイアウト XML の整形式、リソース参照漏れ、ViewBinding のプロパティと id の対応を機械照合

### 未検証（ここから着手すること）

- **実機ビルドが一度も通っていない。** Gradle 同期の時点で失敗する可能性がある
- **アーカイブ同期が実際に効くかどうか。最大の不確定要素**
- 実機での描画、余白、タップ判定などの見た目

---

## 4. 作業の優先順位

### 最優先：ビルドを通す

```bash
./gradlew test
./gradlew assembleDebug
```

依存ライブラリのバージョン不整合や AGP の要求 JDK 差異が出たら、そこで修正する。設計には手を入れない。

### 次：実機にインストールして起動確認

```bash
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb logcat -s AndroidRuntime chromium
```

HyperOS 側で「USB デバッグ（セキュリティ設定）」と「USB 経由でインストール」を有効にする必要がある（Mi アカウントのログインが前提）。

確認項目：

1. ライブ配信のチャットが表示されるか
2. YouTube アプリの共有シートにこのアプリが出るか
3. 文字サイズ、コンパクト表示、明暗切り替えが効くか
4. 表示中に画面が消灯しないか

### 本題：アーカイブ同期の検証

アーカイブ動画を開き、`⏱` でパネルを出し、時刻を入力して `▶` を押す。コメントが流れれば成功。

**流れない場合の対処は `ChatActivity.pushPlayerTime()` の差し替えのみで完結するよう、意図的に切り出してあります。** 他の箇所を触る必要はありません。試す順序：

1. 送信先を `window.parent` にする
2. `document.querySelector('iframe')` を探して、その `contentWindow` へ送る
3. チャットページを `iframe` に包む構成へ変更し、親から送る（構成変更が必要になるため最後）

`WebView.setWebContentsDebuggingEnabled(true)` を一時的に入れ、PC の Chrome から `chrome://inspect` で繋いで、ページ側が message を受け取っているか確認するのが早い。

---

## 5. 未決の分岐：Web アプリへの転向

Web アプリ（GitHub Pages に置く PWA）に切り替える案がある。判断はまだ保留中。

**利点**：ビルド環境も ADB も不要。更新は push のみ。そして `iframe.contentWindow.postMessage()` が YouTube の想定どおりの経路になるため、**アーカイブ同期の不確実性が消える**。

**懸念**：チャット埋め込みには `embed_domain` パラメータが必要で、埋め込み先ページの URL と完全一致しないと開かない。したがって `file://` では動作しない。さらに YouTube の公式ヘルプは「ライブチャットの埋め込みはモバイルウェブでは利用できない」と明記しており、**スマートフォンのブラウザで表示されない可能性がある**。

`experiments/embed-test.html` に判定用の最小ファイルを置いてある。GitHub Pages 等に配置し、`YOUR_DOMAIN` と `VIDEO_ID` を実際の値に置き換えて、**ライブ配信中の動画**でスマートフォンの Chrome から開く。

- チャットが表示された → Web アプリ転向を検討する価値が高い。利用者に報告して判断を仰ぐ
- 表示されない → 現行のネイティブ実装で進める

**この判定を先に済ませると、無駄な作業を避けられます。**

---

## 6. やってはいけないこと

- **Google Play 公開を前提とした作業をしない。** YouTube の利用規約および YouTube API Services Terms は、公式 API を経由しないコンテンツ取得と、コンテンツをプレイヤーから切り離した表示を認めていない。加えて Play には既存サイトを包んだだけのアプリを制限する規定がある。収益化はさらに心証を悪くする。この点は利用者に説明済みで、個人利用に限定する方針で合意している
- チャットへの書き込み機能を作り込まない。Google が WebView 内でのログインを制限しているため実現しない。外部ブラウザへ逃がす `↗` ボタンが対処
- `@チャンネル名/live` 形式の URL 解決を実装しない（動画 ID の取得に追加の取得処理が必要になり、方式の利点を損なう）

---

## 7. コーディング規約

- **コード内のテキストはすべて標準的な日本語で書く。** コメント、`strings.xml`、Toast、ダイアログ文言を含む。口語やキャラクター的な語尾は使わない
- 利用者向けの文言はすべて `strings.xml` に置く。レイアウトや Kotlin へのハードコードは不可
- 画面表示は常時ダーク配色（`Theme.Material3.Dark.NoActionBar`）。日中モードは持たない。手元で暗所利用する前提の意図的な選択
- ロジックを追加する際は、可能な限り Android 非依存に切り出し、`app/src/test/` に単体テストを追加する（`TimeText`、`VideoId` が既存の例）

---

## 8. ファイル構成

```
app/src/main/java/com/tak/ytchatdisplay/
├── MainActivity.kt    配信の指定、共有インテント受け取り、履歴一覧
├── ChatActivity.kt    チャット表示、操作バー、アーカイブ操作盤、同期の送信
├── ReplaySync.kt      仮想再生時計（Handler ベース）
├── TimeText.kt        再生位置の表示と入力解釈
├── VideoId.kt         各種 URL からの動画 ID 抽出
└── Prefs.kt           設定値・履歴・再生位置の保存

app/src/test/java/com/tak/ytchatdisplay/
├── TimeTextTest.kt
└── VideoIdTest.kt

experiments/
└── embed-test.html    Web アプリ転向の可否を判定する最小ファイル
```

パッケージ名は `com.tak.ytchatdisplay`。

---

## 9. 利用者とのやりとりについて

- 名前で呼びかけず、「あなた」または「アンタ」と表現する
- 憶測は憶測として明示する。特に本プロジェクトは未検証箇所が多いため、動作確認していない事項を「動きます」と表現しない
- 解決策には長所と短所の両方を示す
